class TopRatedMovie {
  var id;
  String title;
  String posterPath;
  String backdropPath;
  String originalTitle;
  var voteAverage;
  String overview;
  String releaseDate;

  TopRatedMovie(this.id, this.title, this.posterPath, this.backdropPath,
      this.originalTitle, this.voteAverage, this.overview, this.releaseDate);
}